import React from "react";

const ScatteredRight = ({ image, alt }) => {
  return (
    <div className="scattered-right">
      <img src={image} alt={alt} />
    </div>
  );
};

export default ScatteredRight;
